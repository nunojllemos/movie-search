import React from "react";

const Search = (props) => {
	let content = (
		<form onSubmit={() => {}}>
			<input placeholder='Search...' className='input' type='text' />
		</form>
	);

	return content;
};

export default Search;
